// ============================================================================
// sgemm_v2.cu —— 占位文件：v2 的实现内联在 include/sgemm_v2.cuh，本文件不含代码。
// v2 是模板内核（template<const int BLOCKSIZE>），-rdc=false 下跨翻译单元引用
// __global__ 模板特化已被 nvcc 弃用，故内核与注释都放在头文件、由 main.cu 显式实例化。
// 与 sgemm_v1.cu 不同：本文件只有注释、不产生任何符号，因此无需在
// operators/gemm/CMakeLists.txt 里用 list(REMOVE_ITEM) 排除（被 GLOB 编入目标亦无副作用）。
// 若日后在此写 v2 的独立实现，须先移除 .cuh 中的同名模板定义，避免重复符号。
// ============================================================================
