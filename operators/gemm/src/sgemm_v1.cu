// ============================================================================
// sgemm_v1.cu —— 占位文件：v1 的实现内联在 include/sgemm_v1.cuh，本文件不含代码。
// v1 是模板内核（template<const uint TILE_SIZE>），-rdc=false 下跨翻译单元引用
// __global__ 模板特化已被 nvcc 弃用，故内核与注释都放在头文件、由 main.cu 显式实例化。
// operators/gemm/CMakeLists.txt 用 list(REMOVE_ITEM) 将本文件排除在 gemm 目标之外；
// 保留它只为记录 v1 曾按「独立 .cu」规划。若日后在此写 v1 的独立实现，须同时删掉该排除项。
// ============================================================================
