---
description: 
alwaysApply: false
enabled: true
updatedAt: 2026-09-16T02:43:16.137Z
provider: 
---

# 全仓代码注释规则

## 目标

代码注释用于让维护者快速知道代码当前做了什么、数据如何流动、边界行为是什么。

源码不解释设计原因、算法推导、性能收益或历史结论；这些内容统一写入对应的 README、`notes/` 或 `docs/`。

## 适用范围

本规则覆盖全仓库的 CUDA/C++、公共工具、测试与入口、Python 脚本、CMake，以及后续 Triton 代码。

源码注释使用简体中文；CUDA、C++、Python、CMake 和库 API 名称保留原英文写法。

## 写在哪里

- 文件头：用一两句说明该文件实现或提供什么，不写版本演进、性能分析或长篇背景。
- 声明处：默认不写大段“参数 / 返回值 / 启动约束 / 注意事项”模板。只有函数名和类型不足以表达输入、输出或副作用时，补一条简短契约。
- 函数体：在非直观的操作阶段前写短注释，描述该阶段执行的动作。
- 启动、注册和配置处：在 `main.cu` 或等价入口处，用一行写明当前实际采用的 block、grid、动态共享内存或测试配置。
- README、`notes/`、`docs/`：记录“为什么”、算法推导、优化取舍、硬件背景、性能数据、版本对比和实验结论。

## 函数体注释应写什么

仅标注维护者不容易从局部语句直接看出的操作事实，例如：

```cpp
// 将 A / B 当前 k tile 装入共享内存，越界元素补 0。
```

```cpp
// 计算当前输出 tile 的 TILE_SIZE 次乘加。
```

```cpp
// 仅将合法行列范围内的结果写回全局内存。
```

```cpp
// 汇总各 warp 的部分和。
```

```cpp
// 沿 grid-stride 循环处理后续输入段。
```

共享内存、寄存器缓存、临时缓冲区和坐标映射可以用一句话说明其当前存放的数据或负责的区域，例如：

```cpp
__shared__ float sharedA[TILE_SIZE * TILE_SIZE];  // A 的当前 tile
```

```cpp
const int globalRow = ...;  // 当前线程负责的输出行
```

## 源码中不写什么

以下内容不得作为源码注释保留，应转移到文档：

- 为什么某种实现更快、更省带宽或更少同步；
- warp 合并访问、bank conflict、占用率、寄存器溢出等性能原理；
- 数值稳定性推导、误差来源、容差依据；
- 历史性能数据、加速比、版本横向比较；
- CUDA、C++ 或库 API 的通用教程；
- 逐行复述显而易见的赋值、循环和条件判断。

避免这类注释：

```cpp
// warp 内 threadCol 连续，因此 B 的读取是合并访问。
```

改为只描述动作：

```cpp
// 将线性线程索引拆分为 tile 内行列坐标。
```

性能原因与访问模式分析写入算子 README 或学习笔记。

## 审查标准

修改代码时，审查者只检查以下三点：

1. 非直观操作阶段是否有简短的“做什么”说明；
2. 注释是否与当前代码行为一致，特别是边界处理、数据布局、同步位置和启动配置；
3. 是否把“为什么、推导、性能结论”错误地写进了源码。

注释不能独立于代码维护。代码行为改变时，同一改动必须同步更新相关注释；与任务无关的旧注释不顺带重写，除非该文件正被专项注释整理覆盖。